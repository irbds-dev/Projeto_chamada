{
  "version": 2,
  "buildCommand": "python manage.py collectstatic --noinput",
  "builds": [
    {
      "src": "Chamada/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "Chamada/wsgi.py"
    }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "Chamada.settings"
  }
}